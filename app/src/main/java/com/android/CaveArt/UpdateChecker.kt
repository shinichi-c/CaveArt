package com.android.CaveArt

import androidx.compose.runtime.Composable

/**
 * PUBLIC STUB: This file exists so the project compiles.
 * The actual update logic is kept private.
 */
@Composable
fun AppUpdateHandler(viewModel: WallpaperViewModel, currentWallpaper: Wallpaper?) {
    // This is empty. It does nothing, but it stops the "Unresolved reference" error.
}
